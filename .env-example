# Copy to .env and adjust. Never commit .env.
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1
SERVICE_NAME=campushub-backend

# MongoDB (used from Lab 2 onward)
MONGODB_URI=mongodb://127.0.0.1:27017/campushub
